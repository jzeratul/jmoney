
spring:
  application:
    name: jmoney-service
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    generate-ddl: false
    database: h2
  flyway:
    enabled: false
    baseline-on-migrate: false

  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password:

logging:
  level:
    root: INFO

jasypt:
  encryptor:
    password: test